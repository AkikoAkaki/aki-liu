---
title: "Markdown 渲染压力测试"
date: 2025-12-19
slug: "markdown-rendering-test"
draft: false
---

# Markdown 渲染压力测试

> 本文为一篇综合性测试文档，旨在覆盖常见及边缘 Markdown 语法，用以验证渲染器（包括样式、交互与扩展功能）的行为。

## 1. 基本文本与格式化

这是一个包含多种内联与块级文本格式的段落，示范如下：**加粗文本**、*斜体文本*、***加粗且斜体***、~~删除线~~，以及 `行内代码`（示例：`const a = 1;`）。

外部链接示例： [Hugo 官方文档](https://gohugo.io)
内链示例（站内相对链接）： [我的写作](/writing/)

## 2. 标题层级（H1–H6）

# H1 标题
## H2 标题
### H3 标题（应略小于正文字体）
#### H4 标题
##### H5 标题
###### H6 标题

> 引用块示例：
> > 嵌套一级引用
> > > 嵌套二级引用（测试多层引用渲染与缩进）
> > 回到一级引用

## 3. 列表测试

- 无序列表顶级项 A
  - 嵌套无序项 A.1
    - 深层无序项 A.1.a
- 无序列表顶级项 B

1. 有序列表项 1
   1. 嵌套有序 1.1
      1. 深层有序 1.1.a
2. 有序列表项 2

任务列表（带复选框）：

- [x] 已完成项（应显示已勾选样式）
- [ ] 未完成项（应显示空复选框）
- [ ] 待办：测试与 [Hugo 测试页面](/writing/test-links/) 集成

## 4. 代码块测试

带语言标签（JavaScript）：

```js
// 测试特殊字符：`backticks` <tag> &amp; © ©
function greet(name) {
  return `Hello, ${name} — 渲染测试`; // 模板字符串 + Unicode
}
console.log(greet("世界"));
```

不带语言标签（原样块）：

```
Some raw text: <div class="example">raw &lt;html&gt; & JSON: {"a": 1}
Use backticks: ``` inside a code block is tricky
```

## 5. 数学公式与表格

行内公式示例：爱因斯坦质能方程 $E=mc^2$（用 `$...$` 测试内联解析）。

块级公式示例（`$$ ... $$`）：

$$
\int_0^{\infty} e^{-x^2} \, dx = \frac{\sqrt{\pi}}{2}
$$

标准表格（含对齐）：

| 项目 | 描述 | 数量 |
|:-----|:-----:|-----:|
| 苹果 | 新鲜红苹果 | 10 |
| 香蕉 | 进口香蕉（含 **粗体** 与 _斜体_） | 5 |
| 复杂 | 支持 `inline code` 与多行内容<br>第二行文本 | 2 |

## 6. 多媒体、分割线与注脚

占位图片（外链）：

![示例图片：占位图](https://via.placeholder.com/640x200.png?text=Markdown+Render+Test)

---

注脚示例：这是正文中的一句话，需要说明[^note1]。

[^note1]: 这是注脚的内容，包含 **格式化** 与 `代码段`，用于测试注脚渲染。

## 7. 高级与边缘用例

- 行末反斜杠换行测试：这是第一行  
  这是第二行（应显示为硬换行）

- 表示无效/特殊字符的原文：`<script>alert("x")</script>` 和实体 `&lt; &gt; &amp;`。

> HTML 段落嵌入测试：
>
> <details>
> <summary>点击展开</summary>
>
> 这里是一个 HTML details 块，包含 **Markdown** 与原始 HTML。注意渲染器对嵌套 HTML 的处理。
>
> </details>

---

## 8. 渲染器验证清单（快速参考）

- [ ] 标题层级（H1–H6）是否样式正确且层级清晰？
- [ ] 列表（多级）缩进是否正确？
- [ ] 任务复选框是否可交互（如适用）并样式正常？
- [ ] 行内 & 块级公式是否正确渲染（如果启用 Math 支持）？
- [ ] 代码高亮是否适当（语言标记与无标记行为）？
- [ ] 表格对齐/复杂单元格是否正确？
- [ ] 注脚是否在文末生成并可跳转？
- [ ] 图片加载、分割线、HTML 原样块和 details 元素行为是否预期？

---

如果你想我也把这篇文章的渲染截图（针对亮/暗两套颜色）抓下来并附到仓库里，我可以运行本地 server 并截取页面快照来做进一步的视觉对比测试。
